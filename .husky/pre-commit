#!/bin/sh
# Evident Technologies — pre-commit hook
# Runs lint-staged if installed locally; skips gracefully otherwise.

if [ -f node_modules/.bin/lint-staged ]; then
  npx lint-staged --concurrent false
else
  # lint-staged is not installed — nothing to run.
  exit 0
fi
