# -----------------------------------------------------------------
# Evident — Production Environment Template
# -----------------------------------------------------------------
# Copy to .env and fill in real values.  Never commit .env to VCS.
# -----------------------------------------------------------------

# Flask
FLASK_ENV=production
SECRET_KEY=CHANGE-ME-TO-A-LONG-RANDOM-STRING

# Database (PostgreSQL)
DATABASE_URL=postgresql://evident:CHANGE_ME@localhost:5432/evident

# Gunicorn
GUNICORN_WORKERS=5
GUNICORN_BIND=0.0.0.0:8000
GUNICORN_TIMEOUT=120
GUNICORN_LOG_LEVEL=info

# Storage backend (local | s3)
STORAGE_BACKEND=local
STORAGE_ROOT=/opt/evident/evidence_store

# S3 (only when STORAGE_BACKEND=s3)
# S3_BUCKET=evident-evidence
# S3_PREFIX=prod
# S3_REGION=us-east-1
# S3_ENDPOINT_URL=

# Rate-limiting (Flask-Limiter)
RATELIMIT_STORAGE_URI=redis://localhost:6379/0

# Forwarded headers trust
FORWARDED_ALLOW_IPS=127.0.0.1

# Session
SESSION_COOKIE_SECURE=true
