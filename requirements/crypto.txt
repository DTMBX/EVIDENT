# Evident Technologies — Chain-of-Custody Cryptography
# Evidence hashing, timestamping, digital signatures, tamper detection
#
# Goal: Chain-of-Custody Logger
# Requires: Ubuntu system packages (see docs/apt-packages.md)
#   - libssl-dev, libffi-dev, libsodium-dev
# -------------------------------------------------------------------

-r base.txt

# --- Core cryptographic library ---
cryptography==46.0.4                # hashing, signing, X.509, envelope encryption

# --- Public-key cryptography ---
PyNaCl==1.6.2                       # libsodium bindings — Ed25519, Curve25519, sealed boxes

# --- RFC 3161 timestamping ---
rfc3161ng==2.1.3                    # trusted timestamp authority (TSA) client
# NOTE: Last updated 2020-10. Implements RFC 3161 spec correctly.
# Dependencies: pyasn1, cryptography, requests.
# No actively maintained alternative for RFC 3161 in Python.

# ulid-py and orjson are provided by base.txt:
#   ulid-py  — sortable unique evidence record IDs
#   orjson   — deterministic JSON serialization for hash inputs
